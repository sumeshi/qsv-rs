title: "Test Quilt Configuration for qsv quilt"
description: "A simple test configuration for quilt command"
version: "1.0"
author: "QSV Test Suite"

stages:
  load_initial_data:
    type: load
    source: "../sample/simple.csv"
  
  process_data:
    type: process
    source: "load_initial_data"
    steps:
      select:
        columns: ["col1", "col2"]
      showtable: {}